import ScreenPlaceholder from "../../components/ScreenPlaceholder";

export default function GenerateScreen() {
  return (
    <ScreenPlaceholder
      title="생성"
      description="스타일과 키워드로 이미지를 만들어요. (T2.1에서 구현)"
    />
  );
}
