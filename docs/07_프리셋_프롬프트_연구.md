# 프리셋 프롬프트 연구

버전: v0.2 | 작성일: 2026-07-05 | 갱신: 2026-07-06 (D-007 반영) | 상태: 실험 진행 중

기본 프리셋 6종의 프롬프트·파라미터 실험 기록. 코드상 현재값의 원본은 `src-tauri/resources/presets.default.json`이며, **실험으로 값을 바꾸면 JSON의 `version`을 올리고 이 문서에 실험 근거를 남긴다.** (프리셋은 버전 관리되는 팀 자산 — PRD §1.3)

> v0.2: 기본 프리셋을 그림체(스타일) 축에서 **용도(이미지 타입) 축 6종**으로 교체 (D-007). 스타일 축은 에센스/스타일 LoRA가 담당한다. 기존 그림체 5종 연구는 부록 A(번들 스타일 후보)로 이관.

## 실험 방법

- **고정 조건**: 동일 시드 3개(예: 42, 1234, 20260705) × 표준 키워드 3종으로 생성해 비교. 한 실험에서 변수는 하나만 바꾼다 (prefix/suffix/negative/steps/cfg 중 하나)
- **표준 키워드**: `coffee cup`(사물), `rocket launch`(장면), `smiling robot`(캐릭터) — 프리셋 간 비교를 위해 공통 사용
- **판정**: 각 프리셋의 성공 기준(아래 표)을 시드 3개 모두 충족하면 ✅, 일부만이면 ⚠️, 미달이면 ❌
- **기록**: 실패한 실험도 남긴다 — "무엇이 안 되는지"가 자산

## 현재값 요약 (v1 기준)

| id | 라벨 | steps | cfg | 크기 | 성공 기준 |
|---|---|---|---|---|---|
| default | 기본 | 28 | 6.5 | 1024² | 키워드 충실 반영, 과한 스타일 없음, 깔끔한 구도 |
| storybook | 동화같은 | 28 | 6.5 | 1024² | 파스텔톤 유지, 외곽선 없음, 배경 단순 |
| infographic | 인포그래픽 | 26 | 7.0 | 1024² | 정보 전달 중심의 단순 도해, 제한된 색 수, 밝은 단색 배경 |
| icon | 아이콘 | 24 | 7.5 | 1024² | 단일 오브젝트 중앙 배치, 단순한 형태, 배경 단색·여백 충분 |
| 3d-object | 3D 오브젝트 | 30 | 7.0 | 1024² | 부드러운 라이팅, 클레이 질감, 단색 배경 |
| banner | 배너 | 28 | 6.5 | 1216×832 | 가로 구도, 한쪽에 카피 여백, 시선 끄는 색 대비 |

공통 negative 기반: `text, watermark` (+ 프리셋별 배제 요소)

---

## 1. default (기본)

**현재 (v1)**
- prefix: `high quality digital illustration of`
- suffix: `clean composition, balanced colors, detailed`
- negative: `text, watermark, low quality, blurry`

**가설 / 실험 아이디어**
- [ ] "기본"이 스타일 색채를 최소로 가져가는지 — 에센스/LoRA 스타일과 결합했을 때 suffix가 스타일을 침범하지 않는지 확인
- [ ] `digital illustration` 대신 매체 무지정(`high quality image of`)이 스타일 결합에 더 중립적인지

**실험 기록**

| 날짜 | 버전 | 변경 내용 | 시드 | 결과 (성공 기준 대비) | 판정 | 채택 |
|---|---|---|---|---|---|---|
| | | | | | | |

## 2. storybook (동화같은)

**현재 (v1)**
- prefix: `cinematic illustration of`
- suffix: `creating dreamlike atmosphere, soft pastel colors`
- negative: `text, watermark, photo-realistic`

**가설 / 실험 아이디어**
- [ ] `storybook illustration` prefix가 `cinematic illustration`보다 파스텔톤 일관성이 좋은지
- [ ] negative에 `outline, harsh shadows` 추가 시 외곽선 억제 효과

**실험 기록**

| 날짜 | 버전 | 변경 내용 | 시드 | 결과 (성공 기준 대비) | 판정 | 채택 |
|---|---|---|---|---|---|---|
| | | | | | | |

## 3. infographic (인포그래픽)

**현재 (v1)**
- prefix: `simple flat infographic illustration of`
- suffix: `clear visual hierarchy, limited color palette, geometric shapes, white background`
- negative: `text, watermark, photo, cluttered, realistic shading`

**가설 / 실험 아이디어**
- [ ] SD는 글자를 못 그리므로 negative `text`가 필수인데, "도표 느낌"까지 죽이지 않는지 (`diagram, chart` 계열 키워드로 확인)
- [ ] `isometric` 추가 시 인포그래픽다움이 오르는지 vs 용도가 좁아지는지
- [ ] 색 수 제한: suffix `2-3 accent colors`의 실효성

**실험 기록**

| 날짜 | 버전 | 변경 내용 | 시드 | 결과 (성공 기준 대비) | 판정 | 채택 |
|---|---|---|---|---|---|---|
| | | | | | | |

## 4. icon (아이콘)

**현재 (v1)**
- prefix: `single app icon of`
- suffix: `centered, simple rounded shapes, flat design, solid color background, generous margins`
- negative: `text, watermark, photo, multiple objects, complex background, realistic detail`

**가설 / 실험 아이디어**
- [ ] `app icon`이 iOS 스타일(라운드 사각 프레임)을 과하게 유도하는지 — `flat icon of`와 비교
- [ ] negative `multiple objects`가 복합 키워드(예: "커피와 노트북")를 막아버리는 부작용
- [ ] 투명 배경 다운로드(F-4.2)와의 궁합: 단색 배경이 배경 제거에 유리한 색인지

**실험 기록**

| 날짜 | 버전 | 변경 내용 | 시드 | 결과 (성공 기준 대비) | 판정 | 채택 |
|---|---|---|---|---|---|---|
| | | | | | | |

## 5. 3d-object (3D 오브젝트)

**현재 (v1)** — 구 `3d-render` 연구값 승계 (부록 A 참고)
- prefix: `cute 3d render of`
- suffix: `soft studio lighting, clay material, pastel background`
- negative: `text, watermark, photo, flat illustration`

**가설 / 실험 아이디어**
- [ ] `blender 3d, octane render` 류 렌더러 키워드가 클레이 질감을 해치는지
- [ ] `cute` 제거 시 사물(비캐릭터) 키워드 품질 변화

**실험 기록**

| 날짜 | 버전 | 변경 내용 | 시드 | 결과 (성공 기준 대비) | 판정 | 채택 |
|---|---|---|---|---|---|---|
| | | | | | | |

## 6. banner (배너)

**현재 (v1)**
- prefix: `wide promotional banner graphic of`
- suffix: `dynamic composition, empty copy space on one side, bold color contrast, modern gradient background`
- negative: `text, watermark, cluttered, centered composition`
- 크기: 1216×832 (프리셋 기본값이 가로형 — 크기 UI 선택과의 우선순위는 T2.4에서 확정)

**가설 / 실험 아이디어**
- [ ] `copy space on one side`가 실제로 좌/우 여백을 만드는지, `left side` 명시가 더 안정적인지
- [ ] negative `centered composition`이 구도 다양성을 해치는지
- [ ] 16:9(1344×768)가 SDXL에서 1216×832보다 배너 구도에 유리한지

**실험 기록**

| 날짜 | 버전 | 변경 내용 | 시드 | 결과 (성공 기준 대비) | 판정 | 채택 |
|---|---|---|---|---|---|---|
| | | | | | | |

---

## 프리셋 공통 미해결 질문

- [ ] IP-Adapter(스타일 에센스) 결합 시 프리셋 suffix와 에센스 프롬프트가 충돌하는 조합 목록화
- [ ] 한→영 변환 결과가 프리셋 prefix와 어색하게 붙는 케이스 수집 (관사·복수형)
- [ ] 배너 외 프리셋도 용도별 최적 기본 해상도가 다른지 (아이콘 = 정방형 고정 등)

---

## 부록 A. 번들 스타일 후보 (구 그림체 프리셋 v1 값 보존)

D-007로 기본 프리셋에서 제외된 그림체 5종. **스타일 축**(번들 스타일 LoRA 3종 또는 기본 에센스 프리셋 — PRD Flow A "플랫 일러스트" 예시)의 후보 값으로 보존한다. 채택 시점: M4(에센스) 또는 번들 LoRA 준비 시.

| id(구) | 라벨 | prefix | suffix | negative | steps/cfg |
|---|---|---|---|---|---|
| flat-vector | 플랫 벡터 | `flat vector illustration of` | `minimal shapes, solid colors, clean composition` | `text, watermark, photo, 3d render, gradient` | 26 / 7.0 |
| watercolor | 수채화 | `delicate watercolor painting of` | `soft washes, paper texture, gentle color bleeding` | `text, watermark, photo-realistic, harsh lines` | 30 / 6.0 |
| line-art | 라인 아트 | `clean line art drawing of` | `consistent line weight, minimal shading, white background` | `text, watermark, photo, color fill, gradient` | 24 / 7.5 |
| photo-real | 실사풍 | `professional product photograph of` | `natural lighting, shallow depth of field, commercial photography` | `text, watermark, illustration, cartoon, painting` | 32 / 6.0 |
| (3d-render) | 3D 렌더 | → 용도 축 `3d-object`로 승계 | | | |

당시 실험 아이디어(미실행)도 함께 보존:
- flat-vector: cfg 7.0→8.0 면 색 균일도 / suffix `2-3 color palette` 색 수 제한
- watercolor: steps 30→24 번짐 질감 유지 여부 / `white paper background` 여백감
- line-art: cfg 6.5/7.5/8.5 선 굵기 비교 / negative `color fill`이 부분 채색까지 막는지
- photo-real: `product photograph`의 인물·장면 일반화 (`photograph of`) / steps 32→28 질감 손실
