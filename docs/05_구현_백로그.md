# 구현 백로그
## LocalBrush — Claude Code에 순서대로 투입할 태스크 분해

버전: v0.1 | 사용법: 마일스톤 순서대로, 태스크 1개 = Claude Code 세션 1개 권장. 각 태스크의 AC(완료 기준)를 세션 시작 시 함께 전달.

---

## M0. 프로젝트 뼈대 (선행)

- [x] **T0.1 리포 초기화**: Tauri 2 + React + TS + Vite + Tailwind 템플릿, ESLint/Prettier, `cargo fmt/clippy`, GitHub Actions(lint+test)
  - AC: `pnpm tauri dev`로 빈 창 실행, CI 그린
- [x] **T0.2 레이아웃 골격**: 사이드바 + 5개 라우트 빈 화면, 디자인 토큰(04 §2) Tailwind 설정, 다크/라이트
  - AC: 모든 라우트 이동 가능, 토큰이 클래스로 사용됨
- [x] **T0.3 SQLite + 마이그레이션**: TAD §3.1 스키마, db 모듈, 마이그레이션 러너
  - AC: 앱 실행 시 app.db 생성, `cargo test`에 CRUD 테스트

## M1. 엔진 연동 (핵심 리스크 먼저)

- [x] **T1.1 부트스트랩 설치기**: TAD §7 상태 머신 — uv venv, ComfyUI clone+pip, 모델 다운로드(이어받기), 진행 이벤트
  - AC: 새 기기 시나리오에서 ready까지 무인 진행, 중단 후 재실행 시 이어서 진행
- [x] **T1.2 ComfyUI 프로세스 매니저**: 기동/헬스체크/종료, 크래시 감지 시 1회 자동 재시작
  - AC: 앱 시작 후 `engine_health`가 running=true, 프로세스 kill 시 자동 복구 로그 확인
- [x] **T1.3 워크플로 템플릿 3종 + 슬롯 치환기**: TAD §6, `_slot` 키 기반 주입, 유닛 테스트
  - AC: 템플릿→유효한 /prompt 페이로드 생성 테스트 통과
- [x] **T1.4 generate command E2E**: 프리셋+키워드 → 이미지 파일 → DB 기록 → `gen://done`
  - AC: CLI 수준 테스트 화면에서 1024px 이미지 생성·저장 확인, 취소 동작
- [x] **T1.5 OOM 폴백**: 실패 감지 → 해상도 하향 → SD1.5 폴백 재시도, 사용자 고지 이벤트
  - AC: 인위적 OOM 시뮬레이션에서 폴백 경로 로그 확인

## M2. 생성 화면 (첫 사용자 가치)

- [x] **T2.1 생성 화면 UI**: 04 §4.1 좌패널+결과 그리드, 상태 3종, ⌘↵/Esc
- [x] **T2.2 프리셋 로딩**: presets.default.json 6종, 타입 카드 UI
- [x] **T2.3 한→영 변환**: 용어 사전 + Argos 폴백 (python/translate.py), 고급 패널에 변환 결과 미리보기 (D-008)
- [x] **T2.3b Argos 설치를 부트스트랩에 추가**: venv에 argostranslate pip + ko→en 모델(HF 미러) 다운로드, translate.py 첫 사용 시 지연 설치
- [x] **T2.4 결과 액션**: ♥, PNG 다운로드(→ 다운로드 폴더), 시드 고정 재생성
- [x] **T2.4b 투명 배경 옵션**: rembg(u2net, HF 미러) 배경 제거 후 PNG 알파 저장 — export_image transparent 활성화, 상세 모달 토글
  - M2 AC(통합): "동화같은 + 통나무집" 입력 → 4장 생성 → 1장 다운로드까지 마우스 5클릭 이내

## M3. 갤러리

- [x] **T3.1 갤러리 그리드 + 무한 스크롤** (history_list cursor)
- [x] **T3.2 상세 모달**: 메타 표시·복사, 같은 설정 재생성, 내보내기 3포맷
- [x] **T3.3 검색·필터**: 키워드/스타일/♥ (스타일 필터 칩 UI는 스타일 목록 도입 시 — M4)
- [x] **T3.4 Finder 드래그 아웃** (tauri-plugin-drag)
  - M3 AC(통합, 집 맥 수동 검증 대기): 갤러리 40+장 스크롤·검색·♥ 필터, 상세 모달 액션, Finder 드래그 아웃

## M4. 에센스 스타일 (배민 경로)

- [x] **T4.1 caption/essence 파이썬 스크립트**: Florence-2 서술 + WD14 태그 → 에센스 프롬프트 조합 (python/essence.py, JSON stdout)
- [x] **T4.2 에센스 마법사 UI**: 04 §4.3 3단계 스텝퍼 (샘플 검증 생성은 저장 후 생성 화면에서)
- [x] **T4.3 IP-Adapter 생성 경로 연결**: styles.json kind=essence → sdxl_ipadapter 워크플로 (SD1.5 폴백 시 어댑터 해제 + 고지)
  - M4 AC(집 맥 수동 검증 대기): 참조 이미지 5장 → 10분 내 에센스 스타일 저장 → 해당 스타일로 생성 시 톤 반영 확인(수동 평가 기록)

## M5. 프리셋 자산화

- [x] **T5.1 프리셋 편집기 + 버전 관리** (history 스냅샷, 복원)
- [x] **T5.2 A/B 비교 생성 뷰** (동일 시드 2열)
- [x] **T5.3 내보내기/가져오기** (schemaVersion 검증)

## M6. LoRA 학습 (토스 경로)

- [x] **T6.1 kohya 설치를 부트스트랩에 추가** (선택 설치: 학습 첫 사용 시)
- [x] **T6.2 데이터셋 빌더 + 캡션 편집 테이블** (04 §4.3 학습 마법사 ①②)
- [x] **T6.3 학습 잡 러너**: profiles.toml 3종, stdout 파서(유닛 테스트), 진행/샘플 이벤트, 취소
- [x] **T6.4 학습 대시보드 UI + 완료 시 스타일 자동 등록**
  - M6 AC(집 맥 수동 검증 대기): 이미지 30장으로 fast 프로파일 학습 완주 → 결과 LoRA로 생성 성공

## M7. 마감 & 배포

- [x] **T7.0 최초 실행 부트스트랩 화면** (04 §4.6): 단계 리스트·프로파일 선택(RAM 감지 추천)·진행률·실패 시 로그 열기/재시도 — 백엔드(T1.1)는 완료, UI만 미구현. 그전까지 새 기기는 bootstrap_cli로 설치
  - ※ 실제 새 기기 설치 플로우는 집 맥 수동 검증 대기
- [x] **T7.1 설정 화면** (모델 관리, 캐시, 라이선스 BOM + 안전 네거티브 편집. 저장 위치 변경 F-5.2·업데이트 확인 F-5.4는 후속, 언어 설정은 T7.2)
- [x] **T7.2 i18n 인프라 + 접근성 패스** (04 §7 체크리스트. i18n은 D-010: 언어 설정·사전 인프라 + 공용 컴포넌트 en만, 전체 번역은 v1.0)
- [x] **T7.3 온보딩 빈 상태·에러 문구 정비** (04 §6 톤 — 빈 상태는 기존에 이미 준수, 재시도 버튼 없는 토스트 실패 문구에 다음 행동 추가 + 기술 용어("프로세스")·문법 오류 수정)
- [x] **T7.4 tauri build .dmg + GitHub Release + 무서명 설치 안내 README** (태그 push → draft release, D-011: Homebrew cask는 별도 tap 저장소 [homebrew-localbrush](https://github.com/claire-yunsumin/homebrew-localbrush)에서 관리, 릴리스마다 수동 갱신)
- [ ] **T7.5 수동 QA 체크리스트 실행**: M1~M6 AC 전체 + 8GB 폴백 시나리오
  - 체크리스트 문서화 완료 → [docs/08_수동_QA_체크리스트.md](08_수동_QA_체크리스트.md). 실행 자체는 Apple Silicon 실기기 필요 — 집 맥 수동 검증 대기 (이 회사 컴퓨터는 인텔이라 실행 불가)

## M8. v0.1.x 후속

- [x] **T8.1 온보딩 코치마크 투어**: 최초 실행 시(부트스트랩 통과 후) 화면 요소를 스포트라이트+툴팁으로 순서대로 훑는 투어. 사이드바 → 스타일 → 이미지 타입 → 키워드 → 갤러리 → 생성 버튼(첫 생성 유도 CTA, 완료 시 키워드 입력 포커스). 건너뛰기·이전/다음·Esc 지원, 본 뒤에는 다시 안 나옴(localStorage), 설정 화면 [가이드 다시 보기]로 재실행
  - AC: 최초 실행 시 자동 표시 / 6스텝 모두 실제 요소 하이라이트 / 마지막 스텝 CTA로 닫으면 키워드 입력 포커스 / 재실행 진입점 존재 / 스텝·위치 순수 로직 유닛 테스트

## M9. 성능 최적화 (상세 스펙: [docs/11_성능최적화_계획.md](11_성능최적화_계획.md))

성능 PR 공통 AC: docs/11 §P0.2 벤치 시나리오로 before/after 수치를 PR 본문에 기록. 품질 영향 항목은 동일 시드 A/B 결과 첨부.

- [x] **T9.1 계측 기반**: 생성 파이프라인 단계별 소요를 `logs/perf.log`에 기록(StageTimer) + `gen://done.durationMs`(TAD §5·tauri.ts 동기화), docs/08에 벤치 시나리오 5종 (11 §P0)
- [x] **T9.2 즉효 묶음**: `[profile.release]`, SQLite WAL·인덱스, HTTP 클라이언트 재사용, 사전·워크플로 파싱 캐시, `gen://progress`·`train://progress` 코얼레싱, 파이썬 스크립트 조건부 기록 (11 §P1)
- [x] **T9.3 썸네일 파이프라인**: 생성 시 512px JPEG 썸네일(백그라운드, gen://done 비차단) + 앱 시작 시 기존 레코드 백필(250ms 스로틀) + `decoding="async"` (11 §P2 — WebP→JPEG 사유는 §P2.1)
- [x] **T9.4 추론 가속(안전 항목)**: `--preview-method none`, 부팅 워밍업(64px 1스텝)으로 콜드 스타트 제거 + `engine_health.model_loaded` 구현, `wait_engine_ready` 지수 백오프. 감시 루프 이벤트화는 기각(11 §P3.5), VAE fp16 등 A/B 항목은 실기기 대기
- [ ] **T9.5 프리셋 샘플러·스텝 재조정**: dpmpp_2m/karras A/B 후 프리셋 버전업 (11 §P3.2) — Apple Silicon 실기기 A/B 필요
- [ ] **T9.6 고속 모드(Lightning 8-step)**: 라이선스 확인·BOM 등재 선행, 고급 패널 토글 (11 §P3.4) — 실기기 품질 확인 필요
- [x] **T9.7 kohya 인자 현대화(안전 항목)**: cache_latents(+to_disk), sdpa, persistent workers — 수학 동등 인자만. unet_only+TE캐시·bf16은 품질 영향 가능 → 실기기 A/B 게이트 유지 (11 §P4)
- [x] **T9.8 에센스·캡셔닝 가속**: WD14 CoreML EP(가용 EP 교집합 폴백), Florence-2 MPS fp16 + 실패 시 fp32 자동 재로드, 캡셔닝 진행 [i/total] 스트리밍 (11 §P5 — 실기기 수치 검증 대기)
- [x] **T9.9 프론트 성능**: 라우트 lazy+manualChunks(진입 청크 25KB로 분리), BootstrapGate 스켈레톤, 갤러리 content-visibility·메모화, ResultGrid memo (11 §P6)
- [x] **T9.10 시스템 수납**: 저장소 통계 spawn_blocking 오프로드, 출력물 이동 크로스 볼륨 copy 폴백(move_file) (11 §P7)
  - ※ bf16/fp16/CoreML/워밍업 실측은 Apple Silicon 실기기 필요 — T7.5와 같은 집 맥 수동 검증 게이트

---

## Claude Code 투입 팁

1. 세션마다 `CLAUDE.md` + 해당 태스크의 문서 섹션(예: "TAD §6과 T1.3을 구현해줘")을 지정
2. 태스크 완료 기준은 AC 그대로 요구: "AC를 만족하는 테스트를 먼저 작성하고 구현해"
3. M1이 전체 리스크의 80% — M1.4까지는 UI 없이 최소 테스트 페이지로 검증하고 넘어갈 것
4. 생성 품질 판단(에센스/LoRA 톤)은 사람이 직접 평가하고, 결과를 프리셋 성공 기준 메모에 기록해 자산화
